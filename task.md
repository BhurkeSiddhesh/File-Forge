# Task: Add interactive input and inputs folder

The user wants to provide input file path and password interactively at the beginning of the script and has requested a folder to dump files into.

## Steps
- [x] Create `inputs` folder.
- [x] Modify `pdf_password_remover.py` to prompt for input if arguments are not provided via CLI.
    - [x] Add `input()` calls for path and password.
    - [x] VERIFY: Run the script interactively.
- [x] Update `AGENTS.md`.
- [x] Commit changes.
